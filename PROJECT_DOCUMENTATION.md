# Lumina Blog 项目文档

## 1. 项目概述

### 主要特性

- 🍎 Apple 风格极简 UI 设计
- ✨ iOS 级流畅动画效果
- 🤖 AI 智能问答（基于 Google Gemini）
- 📱 响应式设计
- 🔍 全局搜索功能
- 🛠️ 后台文章管理系统
- 💬 评论系统（支持 localStorage 持久化）
- 🖼️ 图片上传功能（本地上传 + URL 两种方式）

---

## 2. 技术栈

| 技术          | 版本           | 用途     |
| ------------- | -------------- | -------- |
| React         | ^19.2.4        | UI 框架  |
| TypeScript    | ~5.8.2         | 类型安全 |
| Vite          | ^6.2.0         | 构建工具 |
| Tailwind CSS  | (含自定义配置) | 样式框架 |
| Lucide React  | ^0.563.0       | 图标库   |
| Google Gemini | ^1.41.0        | AI 对话  |

---

## 3. 项目结构

```
lumina-blog/
├── index.html              # 入口 HTML（包含 Tailwind 配置和动画）
├── index.tsx               # React 挂载入口
├── App.tsx                 # 主应用组件
├── types.ts                # TypeScript 类型定义
├── constants.ts            # 静态常量数据
├── metadata.json           # 项目元数据
├── package.json            # 依赖配置
├── tsconfig.json           # TypeScript 配置
├── vite.config.ts          # Vite 构建配置
├── .env.local              # 环境变量（API Key）
├── .gitignore              # Git 忽略配置
├── services/
│   ├── geminiService.ts    # Google Gemini API 服务
│   ├── commentService.ts   # 评论服务（localStorage）
│   └── imageService.ts     # 图片上传服务
└── components/
    ├── Header.tsx          # 顶部导航栏
    ├── PostCard.tsx        # 文章卡片组件（带评论数）
    ├── PostDetail.tsx      # 文章详情页
    ├── AdminDashboard.tsx # 后台管理面板
    ├── LoginModal.tsx      # 登录弹窗
    ├── AIChat.tsx          # AI 聊天组件
    ├── SearchOverlay.tsx   # 搜索浮层
    └── App.tsx             # 应用入口组件（内部组件）
```

---

## 4. 核心文件说明

### 4.1 配置文件

#### `package.json`

项目依赖和脚本配置。

```json
{
  "scripts": {
    "dev": "vite",          // 启动开发服务器
    "build": "vite build",  // 生产构建
    "preview": "vite preview"  // 预览构建结果
  }
}
```

#### `vite.config.ts`

- 开发服务器端口：3000
- 支持 `@` 别名指向项目根目录
- 环境变量注入：`GEMINI_API_KEY`

#### `types.ts`

```typescript
// 文章数据结构
interface BlogPost {
  id: string;
  title: string;
  excerpt: string;
  content: string;
  author: string;
  date: string;
  readTime: string;
  category: string;
  imageUrl: string;
}

// 视图状态
type ViewState = 'HOME' | 'POST_DETAIL' | 'ABOUT' | 'ADMIN';

// 聊天消息
interface ChatMessage {
  role: 'user' | 'model';
  text: string;
}
```

### 4.2 数据层

#### `constants.ts`

- **BLOG_POSTS**: 默认博客文章数组（4篇示例文章）
- **APP_NAME**: 应用名称（默认为 "Lumina."）

### 4.3 服务层

#### `services/geminiService.ts`

AI 对话服务，使用 Google Gemini API。

**核心函数**:

```typescript
generateBlogResponse(prompt: string, context: string): Promise<string>
```

**配置项**:

- 模型：`gemini-3-flash-preview`
- API Key 来源：环境变量 `GEMINI_API_KEY`

---

## 5. 组件说明

| 组件                     | 功能                                               |
| ------------------------ | -------------------------------------------------- |
| **Header**         | 顶部导航栏，包含 Logo、菜单、搜索按钮、主题切换    |
| **PostCard**       | 文章卡片，展示标题、摘要、封面图、分类标签         |
| **PostDetail**     | 文章详情页，支持 Markdown 渲染，右下角 AI 问答入口 |
| **AdminDashboard** | 后台管理：添加/编辑/删除文章                       |
| **LoginModal**     | iOS 风格登录弹窗（管理员入口）                     |
| **AIChat**         | AI 对话浮窗，基于当前文章内容问答                  |
| **SearchOverlay**  | 全屏搜索，支持标题/摘要/分类过滤                   |
| **Comments**       | 评论区组件，显示和添加文章评论                     |
| **ImageUpload**    | 图片上传组件，支持本地上传和 URL 输入              |

---

## 6. 功能使用指南

### 6.1 修改网站名称

编辑 `constants.ts`:

```typescript
export const APP_NAME = "My Blog.";
```

### 6.2 修改管理员密码

编辑 `App.tsx` 中的 `handleLoginAttempt` 函数：

```typescript
if (password === 'your-new-password') {
  // ...
}
```

**默认密码**: `admin`

### 6.3 配置 AI 功能

1. 获取 Google Gemini API Key
2. 创建 `.env.local` 文件：

```
GEMINI_API_KEY=your_api_key_here
```

3. 重启开发服务器

### 6.4 添加新文章

1. 点击页面底部的 🔒 锁图标
2. 输入管理员密码登录
3. 在后台填写文章信息（支持 Markdown）
4. 点击保存

### 6.5 修改默认文章

编辑 `constants.ts` 中的 `BLOG_POSTS` 数组。

### 6.6 评论功能

评论系统已集成到文章详情页底部。读者可以：
- 输入昵称和评论内容进行评论
- 评论数据存储在浏览器 localStorage 中

### 6.7 图片上传

在后台管理添加/编辑文章时：
- 支持本地上传图片（最大 2MB）
- 支持直接输入图片 URL
- 支持 JPEG、PNG、GIF、WebP 格式

---

## 7. 环境变量

| 变量名             | 说明                   | 必需             |
| ------------------ | ---------------------- | ---------------- |
| `GEMINI_API_KEY` | Google Gemini API 密钥 | 否（无 AI 功能） |

---

## 8. 运行项目

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

开发服务器默认运行在 `http://localhost:3000`

---

## 9. 样式配置

自定义动画（在 `index.html` 中）：

- `fade-in`: 淡入动画
- `slide-up`: 上滑动画
- `scale-in`: 缩放动画
- `blur-in`: 模糊淡入

主题颜色配置（Tailwind）：

- 主色：Apple 风格中性色
- 深色模式：自动适配系统主题

---

## 10. 常见问题

**Q: AI 功能无法使用？**

> 需要配置 `GEMINI_API_KEY` 环境变量

**Q: 如何更换所有默认图片？**

> 在 `constants.ts` 中修改 `BLOG_POSTS` 的 `imageUrl` 字段

**Q: 数据存储在哪里？**

> 浏览器 localStorage，重启浏览器后数据保留

---

## 11. 待优化建议

* **后端服务**: 建议添加后端服务存储文章数据，而非仅靠 localStorage
* **图片上传**: 添加图片上传功能而非仅支持 URL
* **评论系统**: 添加用户评论功能
* **SEO 优化**: 添加 meta 标签和 sitemap
* **部署配置**: 添加 CI/CD 流程

---

*文档生成时间: 2026-02-15*
